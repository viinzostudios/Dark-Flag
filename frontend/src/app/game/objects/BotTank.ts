// BotTank is now server-side (GameLoopService). This file is kept for reference only.
// The class is intentionally empty — bot logic lives in backend/game-server/src/game/services/game-loop.service.ts

export class BotTank {
  readonly id: string;
  constructor(id: string) { this.id = id; }
}
